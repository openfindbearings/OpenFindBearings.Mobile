<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="OpenFindBearings.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:OpenFindBearings.Mobile"
    xmlns:views="clr-namespace:OpenFindBearings.Mobile.Views"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
    Title="OpenFindBearings.Mobile">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:MainPage}"
        Route="MainPage" />-->

    <TabBar>
        <ShellContent
            Title="首页"
            ContentTemplate="{DataTemplate views:MainPage}"
            Icon="{mi:Fluent Icon=Home28}" />

        <ShellContent
            Title="入驻"
            ContentTemplate="{DataTemplate views:InquiryPage}"
            Icon="{mi:Fluent Icon=Backpack28}" />

        <ShellContent
            Title="我的"
            ContentTemplate="{DataTemplate views:AboutPage}"
            Icon="{mi:Fluent Icon=Person28}" />
    </TabBar>

</Shell>
